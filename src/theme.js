import { createMuiTheme } from '@material-ui/core/styles';

// A custom theme for this youtube app
const theme = createMuiTheme({
    palette: {
      primary: {
        main: '#E11624',
      },
      background: {
        default: '#fff',
      },
    },
  });
  
  export default theme;